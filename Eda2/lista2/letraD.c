#include <stdlib.h>
#include <stdio.h>





int main(){
    int eleitos_quantidade[3];
    celula *votos = malloc(sizeof(celula));
    int x;

    scanf("%d%d%d",&eleitos_quantidade[0],eleitos_quantidade[1],eleitos_quantidade[2]);
    while(scanf("%d",&x!=EOF){
        temp = malloc(sizeof(celula));
        temp->voto = x;
        
    }

    



    return 0;
}